# Copy to .env. Postgres is used in production (e.g. Neon / Vercel Postgres).
# For a local Postgres you can also point this at a docker/local instance.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
